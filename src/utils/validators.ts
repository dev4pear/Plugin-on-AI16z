export function isValidSolanaAddress(address: string): boolean {
  return /^[1-9A-HJ-NP-Za-km-z]{32,44}$/.test(address);
}

export function parseTokenAmount(amount: number): string {
  return amount.toString();
}
